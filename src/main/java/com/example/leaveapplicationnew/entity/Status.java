package com.example.leaveapplicationnew.entity;

// Note: this enum is persisted in Database as EnumType.STRING, modification of existing elements will cause issues.
public enum Status {
    APPROVED, PENDING, REJECTED
}
